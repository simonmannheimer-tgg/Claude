# Never commit the API key
.env

# Reports contain URLs and performance data — keep local
reports/

# Context Mode — local index DB, not shared
context_mode/context_index.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
.uv/
uv.lock
.claude/processed-comments.txt
