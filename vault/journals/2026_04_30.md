# 2026-04-30

- TODO Wire up GSC + Google Workspace OAuth secrets — see [Issue #15](https://github.com/simonmannheimer-tgg/Claude/issues/15)
  - On personal device: run OAuth flows for GSC + GWS, capture credentials JSON
  - Add `GSC_CREDENTIALS_JSON` and `GOOGLE_WORKSPACE_CREDENTIALS_JSON` as Actions secrets
  - Repeat for Codespaces secrets (separate scope)
  - Smoke-test in a Codespace and on claude.ai/code
- Logseq web is desktop-only (File System Access API). Mobile editing path = GitHub Mobile app on `vault/journals/<date>.md`, or Working Copy (iOS) / Pocket Git (Android).
